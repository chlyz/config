#!/usr/bin/env bash

variant="dhm"
# variant="colemak-lyzell"
# variant="colemak"

killall xcape 2&> /dev/null
setxkbmap -layout us -variant "$variant" -option ctrl:nocaps -option shift:both_capslock

xcape -e 'Control_L=Escape'
